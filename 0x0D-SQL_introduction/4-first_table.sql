-- script that creates a table called first_table in the current database in
-- Mysql server, id INT, name VARCHAR(256)
CREATE TABLE IF NOT EXISTS first_table (id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
name VARCHAR(256))
