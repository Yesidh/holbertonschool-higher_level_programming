#include "lists.h"
/**
 * insert_node - function for insert a node in the list
 * @head: head of the list
 * @number: number for insert in the list
 *
 * Return: pendiente...
 */
listint_t *insert_node(listint_t **head, int number)
{
	if (head == NULL)
		return (NULL);
	return (NULL);
}
